{
  "context": "../",
  "runArgs": [
    "-v", "${workspaceFolder}/.devcontainer/.pipenv-cache:/mnt/pipenv-cache",
    "-v", "${workspaceFolder}/.devcontainer/.dep-cache:/mnt/dep-cache",
    "-v", "/var/run/docker.sock:/var/run/docker.sock"
  ],
  "dockerFile": "Dockerfile",
  "workspaceMount": "/workspaces/src/presidio",
  "overrideCommand": false
}